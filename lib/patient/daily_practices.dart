import 'package:flutter/material.dart';

class Daily_Practices extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: SingleChildScrollView(
        child: Text("Daily Practies"),
      ),
    );
  }
}

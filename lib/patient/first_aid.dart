import 'package:flutter/material.dart';

class First_Aid extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        // backgroundColor: Colors.black,
      ),
      body: SingleChildScrollView(
        child: Text(
          "First Aid Page"
        ),
      ),
    );
  }
}
